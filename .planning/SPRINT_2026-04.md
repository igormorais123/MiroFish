# Sprint 2026-04 — Higiene + Phase 3 + Proxy hardening

Início: 2026-04-27.
Status: ✅ ENTREGUE em sessão única (5 commits).

## Commits desta sprint
- `4860e38` chore(repo): arquiva 82 artefatos experimentais
- `1f0acc9` perf(pipeline): paraleliza secoes 4x + reduz Graphiti polling
- `28e7b82` fix(proxy): waitress + MAX_CONTENT_LENGTH 8MB
- `2381a5c` feat(report): Phase 3 QC overlap + gate + Helena 3 cenarios
- `51678d9` docs(state): atualiza v1.2 status

## KPIs Sprint — entregues
- ✅ Pipeline -53% (25min06s → 11min42s) confirmado pre-sprint
- ✅ Overlap relatorio×upload medido em runtime (utils/report_quality.py)
- ✅ Gate editorial por seção implementado (score ≥0.5)
- ✅ Helena com tabela de 3 cenários probabilísticos obrigatória
- ✅ Proxy LLM blindado (Waitress + 8MB cap) — elimina MemoryError
- ⏳ Validação real em pipeline rodando (próxima sprint, com payload-canônico)
- ⏳ Contra-agentes em oasis_profile_generator.py (próxima sprint)

## Concluído nesta sessão

- **Higiene de repo:** 82 arquivos arquivados em `_archive/sprints/sprint-2026-04/` (PNGs, fix_*.py, REL_*.md, comparativos, runs experimentais, _processo_0416.txt, TIMING, GRAPH_ID_FIX). Working tree volta a ser navegável.
- **Causa-raiz dos 4 erros LLM:** ver `logs_proxy.log` linhas 26–37.
  - 4× `HTTP 502` em sequência: upstream Codex CLI falhou (timeout/crash, sem fallback ativo).
  - **`MemoryError` no Werkzeug** ao tentar `rfile.read(10_000_000)` — proxy LLM está em **dev server single-threaded**, sob payload concorrente grande.
- **Phase 3 — descoberta:** `report_agent.py:1371-1375` **já força `min_tool_calls=3` e `max_iterations=5`** com 4 tools (insight_forge, panorama_search, quick_search, interview_agents). O grounding mínimo já existe. Falta:
  1. Medir overlap textual relatório vs upload (alerta >30%)
  2. Gate editorial pós-geração rejeitando seções sem dado numérico/citação
  3. Helena com 3 cenários probabilísticos calibrados
  4. Contra-agentes (devil's advocate) em `oasis_profile_generator.py`

## Próximos passos imediatos

### Fix #1 — Proxy LLM em produção (P0, 30min)
- Trocar Werkzeug por Waitress: `pip install waitress` + `from waitress import serve; serve(app, host='127.0.0.1', port=20128, threads=8)`
- Adicionar `MAX_CONTENT_LENGTH = 5_000_000` no Flask app (limita body)
- Retry interno no proxy quando upstream retorna 5xx (3 tentativas, backoff 2-8s)

### Fix #2 — Medidor de overlap (P0, 2h)
- Novo módulo `backend/app/utils/report_quality.py` com:
  - `jaccard_similarity(a: str, b: str, ngram=5) -> float`
  - `measure_overlap(report_md: str, upload_text: str) -> dict` retorna `{overlap, alert: bool, ngram_matches}`
- Em `report_agent.generate_report` (após assemble final): chamar e anexar ao log + ao MD final como "QC: Grounding Score"

### Fix #3 — Gate editorial (P1, 2h)
- Em `_generate_section_react`, após Final Answer: regex check por:
  - ≥1 número (`\d+%|\d+ \w+|R\$ \d+`)
  - ≥1 citação de nó/entidade (extrair lista do contexto e verificar presença)
- Se falhar, mais 1 iteração ReACT com prompt "sua seção foi rejeitada por falta de dado factual; reescreva citando ≥1 número e ≥1 entidade do grafo"

### Fix #4 — Helena 3 cenários (P1, 3h)
- `_generate_helena_analysis` já existe (L1897). Modificar prompt para forçar JSON estruturado:
  ```json
  { "cenario_base": {"prob": 0.X, "narrativa": "..."},
    "cenario_otimista": {...}, "cenario_contrario": {...} }
  ```
- Validar soma probabilística ≈1.0; renderizar tabela no MD.

### Fix #5 — Contra-agentes (P2, 4h)
- Em `oasis_profile_generator.py`: para cada cenário dominante detectado no grafo, gerar 1-2 perfis com `stance="contrario"` e instruções explícitas de oposição.

## KPIs Sprint
- Pipeline <10min em payload-canônico (atual 11min42s)
- Overlap relatório×upload <30% medido em runtime
- 0 erros 502 no proxy em rodada de smoke (3 runs consecutivos)
- 4 erros LLM/51 → ≤1
